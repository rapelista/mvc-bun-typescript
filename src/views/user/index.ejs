<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>MVC-BUN | All Users</title>
    </head>
    <body>
        <h1>Users</h1>
        <ul>
            <% users.forEach((user) => { %>
            <li><a href="/users/<%= user.id %>"><%= user.name %></a></li>
            <% }); %>
        </ul>
    </body>
</html>
